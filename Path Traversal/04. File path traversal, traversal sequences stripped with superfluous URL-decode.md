## LAB DESCRIPTION

<img width="930" height="438" alt="image" src="https://github.com/user-attachments/assets/5784c267-bf96-4066-a7ce-9b9013081f3a" />

## SOLUTION

---------------------------------------------

- Reference: https://portswigger.net/web-security/file-path-traversal
  
---------------------------------------------

## Analysis

The image retrieval functionality takes a `filename` parameter and resolves it to a filesystem path of the backend.
In this lab, traversal sequences are not accepted in their plain form because the application attempts to sanitize user input.

But the sanitization is performed at the wrong stage of input processing. The request value is URL-decoded by the web server layer and later URL-decoded again by the application code, which means the value is transformed twice before reaching the filesystem API. 

-> In this lab, a standard payload such as `../../../etc/passwd` fails because the filter directly sees `../` sequence and strips them before path resolution. A once-encoded traversal payload such as `%2e%2e%2f` may also fail, because after the server-level decode it already appears as `../`, and the filter removes it.

The logic flaw appears when traversal is encoded one layer deeper so that the first decode does not produce `../` yet.
At that point, the filter does not detect traversal sequences, but the second decode inside application code converts the remaining encoded tokens into active traversal sequences. 

------------------------------------------

## Steps

Start by intercepting a request for opeing an image inside Burp Suite.

<img width="1552" height="405" alt="image" src="https://github.com/user-attachments/assets/15ff31b2-0f17-485b-b7e0-9cbe84a3c6ed" />

Next, we'll forward the captured request to Burp Repeater and start constructing a payload that survives the first decode pass in encoded form, bypasses the string-strip filter, and only becomes `../` during the second decode. 

For this, we'll double encode both the dot and slash characters along with the percentage that forms the traversal sequence.

```urlencode
. --> %2e 
/ --> %2f
% --> %25
```

We will be using the Burp Repeater's in-built encoding feature to encode our payload.

Type in the original payload in the request, then select it  and **Right click -> Convert Selection -> URL -> URL encode all characters** twice.

<img width="1920" height="1120" alt="image" src="https://github.com/user-attachments/assets/04f71364-808e-48cd-ab02-ad9a74cd5e7c" />


First encode:

  <img width="942" height="144" alt="image" src="https://github.com/user-attachments/assets/3f7d1364-aa3e-4eaa-a121-8bd191d1ef72" />

Second encode:
  <img width="943" height="147" alt="image" src="https://github.com/user-attachments/assets/01f3c2f5-38fa-428d-b905-caf8ff87725a" />

--------------------

Now, send this double encoded payload `%252e%252e%252f%252e%252e%252f%252e%252e%252fetc/passwd` through Repeater. 
