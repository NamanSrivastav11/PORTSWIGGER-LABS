## LAB DESCRIPTION

<img width="955" height="721" alt="image" src="https://github.com/user-attachments/assets/0722d069-8439-4d5b-b8db-a424d5851444" />

## SOLUTION

- Reference: [OS Command Injection](https://portswigger.net/web-security/os-command-injection)

---------------------------------------------

## Lab Description and Observations

- Feedback function (`/feedback`) has a blind OS command injection vulnerability.
- The application executes a shell command containing user-supplied details from the feedback form.
- Command is executed **aynchronously** and has no visible impact on the HTTP response.
- You  
- There is a writable folder on the server:
  ```text
  /var/www/images/
  ```

Product images are serverd from this folder via a `/image` endpoint.

Goal: execute `whoami`, redirect its output into `/var/www/images/`. then retrieve the results via the image-loading URL.

------------------------------------------------

The lab uses a blog application with a **Submit feedback** page that has a form with:
- Name
- Email
- Subject
- Message

<img width="1349" height="907" alt="image" src="https://github.com/user-attachments/assets/58731b04-ef2b-4102-a733-cb75ee5d77b4" />


Submitting a normal feedback generates a POST request:
